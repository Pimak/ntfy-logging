# Maven
target/

# Flatten-maven-plugin output — plugin is not used by this build, kept as a belt-and-braces guard
.flattened-pom.xml

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store
Thumbs.db

# MkDocs build output (`mkdocs build`, and the intermediate build `mike deploy` runs).
site/

# Aggregated Javadoc generated into the MkDocs tree by `-P docs-site javadoc:aggregate`
# (see .github/workflows/docs.yml) — build output, never committed.
docs/apidocs/
